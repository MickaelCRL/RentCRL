version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: crrentcrlfc.azurecr.io/app:latest
    ports:
      - "5173:80"
    environment:
      API_BASE_URL: http://localhost:5047